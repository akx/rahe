/* eslint-env browser */
import React from 'react';
import ReactDOM from 'react-dom';
import App from './App';
import css from 'antd/dist/antd.min.css';  // eslint-disable-line

ReactDOM.render(React.createElement(App), document.getElementById('root'));
